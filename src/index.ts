/**
 * DK- Toolkit
 *
 * A comprehensive toolkit for data transformation and utility functions
 * with full TypeScript support and extensive documentation.
 *
 * Main entry point for the package.
 *
 * @example
 * ```typescript
 * import { formatDate, validateEmail, mergeObjects } from 'dk-toolkit';
 *
 * // Format dates
 * const formatted = formatDate(new Date(), 'YYYY-MM-DD');
 *
 * // Validate inputs
 * const isValid = validateEmail('user@example.com');
 *
 * // Transform data
 * const merged = mergeObjects({ a: 1 }, { b: 2 });
 * ```
 */

export * from './utils';
